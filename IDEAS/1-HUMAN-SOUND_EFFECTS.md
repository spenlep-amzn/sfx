1. clone github
2. install it in SpenlepAgents folder
3. Replace global settings.json by symlinking it

```bash
 rm ~/.claude/settings.json && ln -s /Users/notspencer/workplace/claude-poc-sfx/.claude/settings.json ~/.claude/settings.json && ls -la ~/.claude/settings.json
```

4. Update README one-time steps to include this one!
